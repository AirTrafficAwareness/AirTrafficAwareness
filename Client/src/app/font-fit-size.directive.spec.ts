import { FontFitSizeDirective } from './font-fit-size.directive';

describe('FontFitSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new FontFitSizeDirective();
    expect(directive).toBeTruthy();
  });
});
