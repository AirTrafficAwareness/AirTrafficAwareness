import request, { RequestParams } from "../src/request"
import assert from "assert"
// describe("requests", () => {
//     context("Postive Return", () => {
//         it("should return a address not found", async function (){
//             const requestParams: RequestParams = {
//                 qs: {key: "test"},
//                 auth: {username: "test", password: "test"}};
//             const requests = request('https://www.test.com',requestParams);
//             requests.then(res => res).catch(err => assert.equal(err, new SyntaxError('Unexpected token < in JSON at position 0')));
//             });
//         });
//     });

